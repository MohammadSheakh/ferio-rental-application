import { FeatureStatusPage } from '@/components/FeatureStatusPage';

export default function ExpensesPage() {
  return <FeatureStatusPage title="Property expenses" description="Expense vouchers are not connected to the tenant ledger yet. This screen intentionally shows no sample financial data until persisted expense and approval APIs are available." />;
}
